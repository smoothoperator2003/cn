# AIM:-Using, linux-terminal or Windows-cmd, execute following networking commands and note the output: ping, traceroute, netstat, arp, ipconfig, hostname.

ping google.com

traceroute 8.8.8.8.

netstat

arp -a
 
ipconfig

hostname